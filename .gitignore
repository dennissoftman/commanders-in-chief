target/
fuzz/artifacts/
fuzz/corpus/
*.profraw
*.tmp
# Renderer captures written into the working directory when no output path is given.
*.png
*.ppm
!docs/**/*.png
# Committed reference images for the visual regression harness. These are deliberately tracked: the
# harness compares against them, so an ignored reference silently removes the coverage it provided.
!crates/cic-render/tests/references/**/*.png
.idea/
.vscode/
