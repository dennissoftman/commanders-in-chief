# cic-core

Owns dependency-free primitives shared by the workspace. It currently provides bounded
binary input and structured errors.

It must not depend on other project crates or contain format-, rendering-, filesystem-,
network-, or simulation-specific policy.

